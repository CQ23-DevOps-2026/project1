spring:
  cloud:
    gateway:
      server:
        webflux:
          default-filters:
            - SaveSession
          routes:
            - id: nextjs
              uri: http://backoffice-nextjs:3000
              predicates:
                - Path=/**
